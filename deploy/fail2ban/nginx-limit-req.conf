# Optional: Fail2ban filter – Nginx 429 (rate limit) pažeidimai
# Jei nori blokuoti IP, kurie dažnai gauna 429, sukurk:
#   /etc/fail2ban/filter.d/nginx-limit-req.conf
# Turinys žemiau. Tada jail.local įjunk [nginx-limit-req].

[Definition]
failregex = limiting requests, excess:.* by zone.*client: <HOST>
ignoreregex =
